[workspace]
authors = ["Samuel Ibarra <Samuel.E.Ibarra@Dell.com>"]
channels = ["https://prefix.dev/conda-forge", "https://repo.prefix.dev/max-nightly"]
name = "tests"
platforms = ["linux-64"]
version = "0.1.0"
preview = ["pixi-build"]

[dependencies]
mojo = "*"
strata = { path = ".." }

# [package.build]
# backend = { name = "pixi-build-mojo", version = "0.1.*" }

# [package.host-dependencies]
# mojo-compiler = "<=1.0.0"

# [package.build-dependencies]
# mojo-compiler = "<=1.0.0"

# [package.run-dependencies]
# mojo-compiler = "<=1.0.0"

[tasks]
test_runners = "mojo test_runners.mojo"
test_docs = "mojo test_docs_completeness.mojo"
tests = {depends-on = ["test_runners", "test_docs"]}
